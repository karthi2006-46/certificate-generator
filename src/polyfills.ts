/**
 * This file includes polyfills needed by Angular and is loaded before the app.
 * You can add your own extra polyfills to this file.
 *
 * This file is divided into 2 sections:
 *   1. Browser polyfills. These are applied before loading ZoneJS and are sorted by browsers.
 *   2. Application imports. Files imported after ZoneJS that should be loaded before your main
 *      file.
 *
 * The current setup is for so-called "evergreen" browsers; the code falls back to the old
 * polyfill bundles if needed (IE 11).
 */

/**
 * By default, zone.js will patch all possible macroTask and (eventTarget) sources before it is
 * loaded, but will throw an error if such patching has already been performed. If you have other
 * global libraries that also patch macroTask/eventTarget, KEEP ZONE.JS ON TOP, so zone.js patches
 * the Internet more natively (see bugs #31394).
 *
 * With window['ngZone'] you can disable the patch of specific APIs by setting `ng.core.NgZone.isCheckEventListenerFun = false`
 */
